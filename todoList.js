const todos = [
  'Stripe Management', //done ig

  'do we need type string in jwt-payload role',
  'check if pagination exits in all get APIs',
  'add filters to the search queries',
  
  'handle incoming files and types', //done ig
  'try making page and limit default if not provided', // done ig
  'ParsePipes are not optional', //done ig
  'you have optional pipe code in slack + query params can also have DTOs', //done ig
  'after payment token isnt updated so just make the token thing dynamic', //done ig
  'sync stripe and local packages', //done ig
  'need me a function that can recompute the jwt token and get newly added props as well for our user', //done ig
  'manage access rights for media',


  'try out indexing the intent id in payment intent schema',
  'Testing out something in user controller, appraently i can apply guards to the whole class as well',
];

// Generate dependency graph using madge
// madge --image graph.png --extensions ts src
